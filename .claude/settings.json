{
  "permissions": {
    "allow": [
      "Skill(run)",
      "Skill(run:*)",
      "Bash(git -C /mnt/d/maxtellar ls-files -ci --exclude-standard)",
      "Bash(xargs -0 -r git rm -r --cached -q --ignore-unmatch)"
    ]
  }
}
